<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user">
	<insert id="join">
		insert into tbl_user (user_id, user_password, user_name, user_age, user_gender)
		value(#{userId}, #{userPassword}, #{userName}, #{userAge},#{userGender})
	</insert>
</mapper>